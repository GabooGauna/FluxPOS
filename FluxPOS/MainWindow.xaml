<Window x:Class="FluxPOS.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FluxPOS"
        mc:Ignorable="d"
        Title="FluxPOS" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="10">
            <TextBlock Text="Nombre del Producto:" FontWeight="Bold"/>
            <TextBox x:Name="txtNombre" Height="30"/>
        </StackPanel>

        <StackPanel Grid.Row="1" Margin="10">
            <TextBlock Text="Precio:" FontWeight="Bold"/>
            <TextBox x:Name="txtPrecio" Height="30"/>
        </StackPanel>

        <Button Grid.Row="2"
                Content="Agregar producto"
                Width="200"
                Height="50"
                FontSize="20"
                VerticalAlignment="Top"
                Margin="0,20,0,0"
                Click="Button_Click"         
            />

        <ListBox x:Name="lstProductos"
                 Grid.Row="2"
                 Margin="10, 80, 10, 10"
                 MinHeight="100" />

        <Button x:Name="btnEliminar"
                Grid.Row="2"
                Content="Eliminar Seleccionado"
                Width="130"
                Height="35"
                VerticalAlignment="Top"
                HorizontalAlignment="Right"
                Margin="0,40,12,0"
                Background="Salmon"
                Click="btnEliminar_Click"
                FontWeight="SemiBold"
            />

        <TextBlock
            x:Name="lblTotal"
            Grid.Row="2"
            Text="Total: $0.00"
            FontSize="22"
            FontWeight="SemiBold"
            VerticalAlignment="Bottom"
            HorizontalAlignment="Right"
            Margin="20"
            />
    </Grid>
</Window>